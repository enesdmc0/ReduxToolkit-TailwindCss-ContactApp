import React from 'react';

const Header = () => {
    return (
        <div className="w-full h-16 flex items-center justify-center">
            <h1 className="font-bold text-2xl">contact's app</h1>
        </div>
    );
};

export default React.memo(Header);
